# Production API (committed so Vercel builds outside Vercel's env UI still get it).
# Next.js auto-loads this file when NODE_ENV=production (Vercel sets it).
NEXT_PUBLIC_API_URL=https://personal-backend-server.vercel.app/api
