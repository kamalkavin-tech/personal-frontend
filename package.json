{
  "name": "personal-frontend",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @vaultx/shared && npm run dev --workspace @vaultx/web",
    "build": "npm run build --workspace @vaultx/shared && npm run build --workspace @vaultx/web",
    "typecheck": "npm run typecheck --workspace @vaultx/shared && npm run typecheck --workspace @vaultx/web",
    "start": "npm run start --workspace @vaultx/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
